# Backend URL that the frontend connects to (Socket.IO)
# Set this to your server's public IP/domain when deploying
# Leave empty to auto-detect from browser hostname (default: http://<hostname>:26033)
# Socket.IO connects to this URL with path /api/socket.io
NEXT_PUBLIC_BACKEND_URL=
